# Analytics — defaults are baked into src/config/site.ts, override here if the
# properties change.
NEXT_PUBLIC_GA_ID=G-S8LD7KGECB
NEXT_PUBLIC_GTM_ID=GTM-PBGS65KL

# Google Search Console verification. A working token is committed as the
# default in src/config/site.ts, so this only needs setting if the property is
# re-verified and Google issues a new one.
NEXT_PUBLIC_GOOGLE_VERIFICATION=
